export enum AuthErrors {
  LOGIN_ERROR = 'Invalid username or password',
  JWT_AUTH_ERROR = 'Invalid or expired token',
}

export const JWT_SECRET = 'super-secret';

export const JWT_HEADER = 'token';

export const SIGNUP_BONUS = 500;
